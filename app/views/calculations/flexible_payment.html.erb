<h1>
  Flexible Payment
</h1>

<p>

 A <%= @number_of_years.round(0) %> year loan of <%=number_to_currency(@loan_value) %>, with an annual rate of <%= number_to_percentage(@the_basis_points.round(1)) %>, requires a monthly payment of <%=number_to_currency(@monthly_payment) %>.
</p>
